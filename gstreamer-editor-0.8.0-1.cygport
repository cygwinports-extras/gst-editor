inherit gstreamer

src_compile() {
	cd ${S}
	gstreamer_cvs_checkout -r BRANCH-GSTREAMER-0_8 common
	mkdir -p m4
	gstreamer_autoreconf

	cd ${B}
	cygconf --disable-static
	cygmake
}

src_install() {
	cd ${B}
	cyginstall

	rm -fr ${D}/usr/include/ # ${D}/usr/lib/
}
